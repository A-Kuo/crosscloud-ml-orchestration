# Core ML / NLP
torch>=2.2.0
transformers>=4.40.0
numpy>=1.26.0
scipy>=1.13.0
scikit-learn>=1.4.0

# Inference server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
httpx>=0.27.0

# Cloud SDKs (optional; mocked in tests)
google-cloud-bigquery>=3.20.0
google-cloud-firestore>=2.19.0
boto3>=1.34.0

# Airflow (local dev; install separately via pip install apache-airflow)
# apache-airflow>=2.9.0
# apache-airflow-providers-google>=10.17.0
# apache-airflow-providers-amazon>=8.20.0

# Observability / logging
structlog>=24.1.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
